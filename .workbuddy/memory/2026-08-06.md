# 2026-08-06

## Vercel 构建报错修复（.bin/vite Permission denied, exit 126）
- 现象：Vercel build 报 `sh: .../xcsc-site/node_modules/.bin/vite: Permission denied`，`npm run build` 退出码 126。`Previous build caches not available` 仅为提示，非根因。
- 根因（修正）：Vercel 项目根目录实际设为 `xcsc-site`，所以它直接跑 `xcsc-site/package.json` 的 `build`（`vite build`），而非根目录包装脚本。`.bin/vite` 在 Vercel 冷安装（Previous build caches not available）时丢失 exec bit → Permission denied。之前根目录包装脚本的改动没被用到。
- 修复：改 `xcsc-site/package.json` 的 build 为 `node node_modules/vite/bin/vite.js build`（绕过 .bin 权限）；根 `package.json` 同步成 `cd xcsc-site && npm install && node node_modules/vite/bin/vite.js build && rm -rf "$(cd .. && pwd)/dist" && cp -r dist "$(cd .. && pwd)"`。本地 `npm run build` 验证：`✓ built in 655ms`，出包正常（此前本地失败是 WorkBuddy 的 genie-safe-delete 拦截 trash dist，Vercel 无此问题，忽略）。
- 未提交（用户自行 git push / Vercel 重新部署）。

## 新增「返回顶部」悬浮按钮
- 需求：用户划到页面最后一页（底部）后显示返回顶部按钮。
- 新增 `xcsc-site/src/components/BackToTop.jsx`：固定右下角圆形按钮，滚动距页面底部 ≤400px 时 `is-shown` 浮现（requestAnimationFrame 节流 scroll/resize 监听），点击 `window.scrollTo` 平滑回顶；尊重 `prefers-reduced-motion`。含 aria-label、tabIndex 切换、focus-visible 描边。
- `App.jsx` 引入并在 `<Contact />` 之后渲染 `<BackToTop />`。
- 样式加在 `sections.css` 末尾 `.to-top`（cobalt 蓝底、ice-white 箭头 SVG、pill 圆角、渐显过渡、720px 以下缩小并贴近边缘）。
- 构建验证：`npm run build` ✓ built（49 modules）。未提交（用户自行 git push）。

## 修复「返回顶部」只上移一点、不到顶
- 现象：点按钮只往上移一点点，不到顶部。
- 根因：`global.css` 中 `body { overflow-x: hidden }` 触发 CSS 规范——`overflow-x:hidden` 且 `overflow-y:visible` 时，`overflow-y` 被算成 `auto`，**body 变成真正滚动容器**，window 不再滚动；`window.scrollTo({top:0})` 滚的是 documentElement（无溢出）故无效。
- 修复：① `global.css` 的 `body` 由 `overflow-x: hidden` 改为 `overflow-x: clip`（clip 只裁剪、不创建滚动容器，窗口重新成为唯一滚动源）。② `BackToTop.jsx` 的 `toTop` 增加兜底：同时 `document.body.scrollTo(opts)`。
- 构建验证：`npm run build` ✓ built。未提交（用户自行 git push）。

## 「返回顶部」改为「跳转首页」（滚动不可靠，改用导航）
- 用户反馈滚动回顶在部署环境仍不可靠，要求点击直接跳转首页。
- 改动 `BackToTop.jsx`：`toTop` 改为 `window.location.href = '.'`（跳到当前目录索引页=网站首页，兼容根目录与子路径部署）；aria-label `返回顶部`→`返回首页`；顶部注释同步更新。滚动相关逻辑（监听、scrollTo、overflow-x:clip）保留无害。
- 构建验证：`npm run build` ✓ built（49 modules）。未提交（用户自行 git push）。

## 首页右上角新增「北京时间」读数（实验性）
- 新增 `xcsc-site/src/components/Clock.jsx`：`Intl.DateTimeFormat('zh-CN', {timeZone:'Asia/Shanghai', hourCycle:'h23', ...})` 取 UTC+8 时间，`setInterval` 每秒刷新；渲染 `.hero__clock`（标签+ HH:MM:SS）。
- `Hero.jsx` 引入 `<Clock />`，作为 `.hero` 直接子元素（绝对定位相对 hero）。
- `sections.css` 加 `.hero__clock` 样式：绝对定位 `top: calc(var(--nav-h)+28px); right: var(--gutter)`（与取景框右上角对齐），mono 字体、value 用 `--ice-white`+tabular-nums+淡蓝辉光，label 用 `--steel`；`@media(max-width:720px)` 缩小字号防重叠。
- 构建验证：`npm run build` ✓ built（50 modules）。未提交（用户自行 git push）。

## 新增「flag 配置页」彩蛋（/flag.html）
- 需求：在站点根目录新增 flag 配置页，里面写 `ZmxhZ3t3ZWxjb21lX3RvX1hDU0N9`。
- 该 base64 解码 = `flag{welcome_to_XCSC}`（已 base64 -d 验证）。
- 实现：新建 `xcsc-site/public/flag.html`（静态页面），Vite 构建时自动 copy 到 `dist/flag.html`，随部署落到站点根 `/flag.html`。页面套用站点暗色 token 主题（midnight observatory / cobalt），mono 字体，`noindex`，以 config 面板形式展示 `flag: ZmxhZ3t3ZWxjb21lX3RvX1hDU0N9` 与 `encoding: base64`。
- 构建验证：`npm run build` ✓ built，dist/flag.html 已生成。未提交（用户自行 git push）。
- 注意：纯静态页，无 JS 依赖；属隐藏彩蛋，页内/站点未加入口链接。

## 首页 headline「漏洞」点击跳转彩蛋页
- 需求：点击首屏标题里高亮的「漏洞」二字，跳转到彩蛋页 `/flag.html`（code 配置页）。
- 改动：
  - `src/lib/text.jsx`：`parseAccent(str, links)` 新增可选 `links` 参数 `{ 高亮词: url }`，命中时把该词渲染为 `<a class="accent accent--link" href={url}>`，否则仍是 `<span class="accent">`。
  - `src/content/site.js`：`hero` 加 `accentLinks: { 漏洞: 'flag.html' }`（相对路径，兼容 base:'./' 与子路径部署）。
  - `src/components/Hero.jsx`：标题渲染改为 `parseAccent(line, hero.accentLinks)`。
  - `src/styles/global.css`：`.accent` 后加 `.accent--link`（cursor:pointer、底部细下划线、hover/focus 加亮、focus-visible 描边，保证可访问性）。
- 构建验证：`npm run build` ✓ built（50 modules），dist/flag.html 与 bundle 中均含 `flag.html`。未提交（用户自行 git push）。
